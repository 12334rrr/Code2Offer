# 修复实施记录(0.3.0)

对应 `audit/综合审计报告.md` 的缺陷清单,逐项记录修复方式与验证手段。实施日期:2026-09-15。

**总体验证**:`tsc -p .` 零错误;`tsc -p vscode/tsconfig.json` 零错误;新增测试套件 **46/46 通过**(`npm test`);esbuild 打包成功。

## P0 安全

| 编号 | 修复 | 验证 |
|---|---|---|
| S-1 密钥文件进模型输入 | profiler 新增敏感文件拒绝清单(`.env*`/私钥/凭据类,独立于 .gitignore)+ 内容级密钥特征扫描(`sk-`/`BEGIN PRIVATE KEY`/AKIA 等,≤100KB 文本);`RepoFacts.skippedSensitive` 记录被跳过文件并在日志透明展示 | 测试:profileRepo 在含 `.env`/`id_rsa`/`cert.pem`/`credentials.json` 的临时仓库中全部拦截,`app.js` 正常入选 |
| S-2 仓库可重定向请求端点 | `loadConfig` 重写:来源分级(环境变量=1/宿主 .env=2/仓库 .env=3),**BASE_URL 只接受 rank ≤ 密钥来源的来源**——环境密钥 + 仓库端点的组合被拒;BASE_URL 强制 HTTPS(localhost 例外);超时钳制 [10s, 10min] | 测试:6 个 loadConfig 用例覆盖绑定规则、HTTP 拒绝、localhost 例外、overrides 优先级、缺密钥报错 |
| S-3 跨仓库污染 + 优先级颠倒 | 扩展不再写 `process.env`,`resolveConfig` 一次一解析经 `RunOptions.config` 显式传参;CLI/扩展/evaluate 统一走新 loadConfig(trustedDirs + repoDir);新增 `env` 注入参数(测试隔离) | 测试:loadConfig 优先级矩阵;扩展逻辑由类型检查 + 代码审查保证 |
| S-4 .gitignore 缺 .env | 补 `.env`、`vscode/*.vsix`、audit 产物目录 | 文件检查 |

## P1 正确性

| 编号 | 修复 | 验证 |
|---|---|---|
| R-1 门控哈希失真 | 画像哈希 = 文件清单 + 精读文件内容哈希;精读 = overview + chunks 全文;出题 = 上游内容;校验 = 题目全文 + chunks + 提示词 + 模型;stage1 缓存键补 overviewJson;stage4 缓存键用题库清单全文 | 设计审查 + 类型检查;等长改码场景由内容哈希覆盖 |
| R-2 断点按题号复用 | checkpoint 每条带 `inputHash`(题干+答案+引用+摘录的 sha1);加载时只复用指纹一致且 ID 属当前题库的条目;题库重生成时 `resetVerifyCheckpoint` 删除断点;幻觉 ID 不再入库 | 代码级;指纹算法在 stage3Verify |
| R-3 校验失败标 pass | 新增 `unverified` 状态:模型失败/漏答/非法裁决(verdict 白名单)的题标 unverified 不计 pass;确定性错误优先于模型 pass;unverified>0 时阶段不记完成,重跑自动补验 | 代码级 + 报告模板含 unverified 计数 |
| R-4 flag 重写无闭环 | 无代码依据的 flag 题不重写(保留人工复核);重写后 verifyNote 保留原始裁决(`原标红:… → 已重写`);runner 在 3.5 后统一重算校验报告 | 代码级 |
| R-5 100 题配额漂移 | `computeDeficitSlots`(coverage)统一缺口算法:runStage2 补题轮与 topUpToQuota 共用;coerceQuestion 强制以 slot 的类别/难度/目标为准(模型回显不采信);类别枚举注册进 validateQuestion;topup 受配额上限约束、ID 从最大编号续 | 测试:配额恒 100、缺口计算(未知类别不占配额、满配额无缺口)、coerce 强制纠正 |
| R-6 stage4 毒缓存死锁 | `normalizeJdAnalysis` 形状收敛(字符串→数组拆分)+ 可用性校验;坏形状重试一次;只有合格结果才 `cache.set` | 代码级(遵循 stage2 修复环既有模式) |
| R-7 取消不生效 | `RunOptions.abort` → 各阶段 ctx → deepseek 每请求组合信号(超时+外部取消);扩展 withProgress `cancellable:true`,CancellationToken→AbortController;所有选择器 `undefined` 立即返回 | 代码级 + deepseek 组合信号实现 |
| R-8 同目录并发 | runner 运行锁:进程内 Map + 磁盘 `.run-lock`(PID+时间戳,45min 过期);扩展侧 running 标志 + 提示 | 代码级 |

## P2 质量(按主题)

- **性能**:readLines 缓存修复(命中返回内容,不再重复读盘);profiler 单遍读取(内容 Map 复用,LOC/路由/DB/测试证据共用一次 IO);git 热点 `-n 800` + 20s 超时;stage1 模块并发 3;stage5 四文档 `Promise.all`;报告搜索 150ms 防抖 + 侧栏仅 mark 后增量更新。
- **引用边界**:统一 `splitFileLines`(去尾空行);`1 ≤ start ≤ end ≤ total` 严格判定;deterministicCheck 同时查 start;sanitizeCitations 完全越界(start > total)整段删除,不再产出 `120-2` 倒置区间。测试:末尾后一行拒绝、倒置删除、部分越界截断。
- **gitignore**:重写匹配器——前导 `/` 锚定、字符组 `[0-9]`/`[!a]`、globstar、子目录层叠规则、取反;无法解析的规则记入 notes。测试:6 个语义用例全过。
- **报告**:全文无内联事件处理器(data-* + 事件委托,nonce CSP 可生效);localStorage 键含仓库命名空间;搜索索引补追问/加分/易错/对比文本;先截断后转义;unverified 徽标。测试:无 onclick 断言、转义断言、键隔离断言。
- **Markdown**:sanitizeMdCell(`\|` 转义、换行压平)用于对比表与全部行内字段。测试覆盖。
- **健壮性**:stage1 空卡不入缓存、知识卡降级兜底(不抛异常);stage4/topup/rehearse/evaluate 安全 JSON 读取;排练逐题落盘 + `:show` 可连用 + 评分钳制;evaluate items 数组防护、分数钳制、缺失剔除、评委失败按未评估归一化(消双重惩罚)、评委结果缓存(可复现)、引用文件白名单(防路径穿越读任意文件)。
- **CLI**:旗标表解析(布尔旗标不再吞位置参数、`--force <路径>` 不再失效)、值旗标缺参报错、`--version`/`--help`、数值校验。测试:6 个用例。
- **扩展**:设置项模型真实生效(getConfiguration → overrides);openSettings 用 `context.extension.id`;openReport/openOutput 记住最近产物目录(globalState);TreeDataProvider EventEmitter 实时刷新;LogOutputChannel 承载全部核心日志;完成提示区分宿主(vscode 不再让用户跑不存在的 dist/cli 命令);Webview CSP 注入对 `<head>` 大小写容错、script 数量校验,结构异常回退系统浏览器。
- **其他**:STAGE2_TOPUP_USER_HINT 真正生效(补题轮附加提示);选型题模块卡按目标关键词匹配;`.env` 解析支持行内注释;错误消息密钥打码;429/5xx 尊重 Retry-After;401/403/400 立即失败。

## P3 工程化

- 测试从零到 46 例(`npm test` = build + node --test);版本 0.3.0;`.vscodeignore` 排除源码/esbuild/tsconfig/sourcemap/历史 vsix;esbuild 生产不出 sourcemap;prepublish 加类型检查门禁;CHANGELOG.md 新增;README/CLAUDE.md 与实现对齐(优先级、安全规则、命令、版本)。
- **未做(需要用户决策)**:LICENSE 文件与 license 字段(授权选择是法律决定);扩展图标(需要设计资源,建议 128×128 PNG);`repository`/`bugs` 字段(需要公开仓库地址);真实 Extension Host 里的手工安装点击测试。

## 发布产物验证(2026-09-15 补充)

- `npm test` 的 `node --test dist/tests/` 在 Node 24.19 不展开目录参数(假失败),已改为 `node --test "dist/tests/*.test.js"` → **46/46 通过**。
- 新增 `audit/activation-smoke.cjs`:从 vsix **解出实际出货的** `extension.js`,mock VSCode API 后走真实路径,9 项断言全过——activate 注册(4 命令/输出通道/树视图)、树 4 条目、空窗口/无密钥/未生成三类错误提示、openSettings 用真实扩展 ID、报告 Webview 的 nonce CSP 注入与畸形模板回退、**包内文件与本地构建 sha1 一致**。
- 包内容密钥扫描:`extension.js`/`readme.md`/`package.json` 对 `sk-…`/私钥头/AKIA 模式 **0 命中**;vsix 共 7 文件,无 `.env`、无源码、无 sourcemap。
- 商店页文档(`vscode/README.md`)此前仍是 0.2.x 旧密钥优先级(与安全修复矛盾),已重写为 0.3.0 版(正确优先级、取消/输出面板/运行锁、安全声明、FAQ);重写后重新打包并复跑冒烟。

## 遗留说明

- `audit/core-regressions.cjs`、`pipeline-repro.cjs`、`extension-regressions.cjs` 是前轮审计的**缺陷复现脚本**(断言缺陷存在)。修复后它们的部分断言会失败——这是预期(缺陷已消失),不能当作回归失败;后续应将其改写为产品断言并入 `src/tests/`。
- 画像门控对"未被精读且行数不变的文件修改"仍不敏感(权衡:全仓库内容哈希成本);精读文件与文件清单已覆盖,实际风险低。
