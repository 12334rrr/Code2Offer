# Changelog

## 0.9.1(2026-09-17)

报告内交互大升级 + 架构图成品化。

- **分层架构图成品化**:暗色大图样式(标题区 + 徽章 + 层容器虚线 + 节点双行卡片 + 语义边色 + 图例),并**内嵌报告顶部**。
- **点击架构节点 → 词典式抽屉**:显示该模块关联的全部面试题,点击题号直达。
- **代码依据点读**:题目里的 文件:行号 现在可点击——右侧抽屉直接展示该处**真实源码摘录**(带行号),并列出引用同一处代码的关联题目。源码摘录在总装阶段确定性生成,零额外 token。
- **题量再缩一档**:经济 20 / 平衡 40 / 深度 60(仍可自定义 10~100)——题不在多,每题更聚焦好代码与真实痛点。
- 网关实测说明:`deepseek-v4.1-flash` 在 DeepSeek 网关不存在(可用名仅 `deepseek-flash` / `deepseek-v4-pro`),维持 `deepseek-flash`(即网关快档,同类最省)。

## 0.9.0(2026-09-17)

走向 1.0:分层架构图(零成本)+ 商用基建。

- **新增分层架构图**:每次生成自动产出 `架构图.svg`(浏览器直开)与 `架构图.drawio`(可在免费的 diagrams.net 打开继续编辑、导出 PPT)。由仓库画像确定性推导(入口 → API/路由 → 业务模块 → 数据与配置,按真实文件归属连线),**零 token 成本**、无幻觉。
- **商用基建**:新增《隐私政策》《服务条款与费用说明》;商店页补 CI/版本/License 徽章与在线 Demo 报告(GitHub Pages,免安装免 Key 试用);缺 Key 报错现在提供「申请 DeepSeek Key / 3 分钟上手说明」直达按钮。
- **商店检索优化**:分类调整(Education/AI/Programming Languages),关键词扩充(八股文/求职/offer/校招/简历/架构图等),描述突出「带行号证据、对抗校验不编造」差异化。
- **模型兼容性诚实声明**:提示词与路由针对 DeepSeek 家族深度调优;OpenAI 兼容端点协议层可用但未做跨模型质量校准。

## 0.8.2(2026-09-17)

题量自适应与 token 优化(基于真实自跑日志:96 次调用 / 114 万输入 token / 16 分钟,大量消耗在配额缺口补题与宽引用修复上)。

- **目标题量自适应**:经济 30 / 平衡 60 / 深度 80,不再硬凑 100;生成时可自定义 10~100。配额矩阵等比缩放(最大余数法,11 类别广度不丢、每类保底),缺口补题从"动辄 26 题"降到个位数——**补题与修复轮的调用量显著下降**。
- **出题聚焦**:新增规则——题目围绕设计良好的实现与真实开发痛点展开,禁止围绕构建产物/锁文件/审计日志/变更记录设计整题;对比题优先取材该项目的真实备选方案与权衡。
- **锁文件出黑名单**:`package-lock.json` 等依赖锁文件不再进入精读清单与热点排名(此前靠 git 提交次数混进精读,白烧 token)。
- **校验批次更抗断**:批次解析失败(输出截断)自动对半拆批重试,把「未覆盖」题压到接近 0,避免下次运行重复付费。
- 修复轮上下文瘦身(单题 JSON 上限 20000→8000 字符)。

## 0.8.1(2026-09-16)

交付与质量基建(回应独立评分的扣分项):

- **MIT LICENSE** 与商店图标(128×128,深色代码书尖括号设计)——两包 license 字段与 `icon` 字段齐备。
- **CI 常驻**(GitHub Actions):push/PR 自动跑 全量单测 → 缺陷回归门禁 → mock 合约 → 扩展打包 → 激活冒烟,全程无需密钥。
- **缺陷回归门禁**:`audit/regression-gate.cjs` 保证 0.3.0 审计的 15 个历史缺陷探针保持"不可复现",任何一条复现即拦截发布。
- 依赖安全审计:双包 `npm audit` 0 漏洞。
- 0.8.0 题库契约首次真实运行实测(economy,20 次调用/缓存命中 56%):**38/38 追问带参考要点、38/38 难度分(2-9 真实分布)、宽引用 0、flag/unverified 0**——上一代产物为 0/100 参考要点、17 条宽引用;economy 模式门禁 B 属预期(非 100 题完整题库)。
- **S 级验收循环(6 轮真实深度模式迭代)**:新增阶段 3.6「风险给药定向修复环」(确定性触发 + 形状校验拒绝无给药结果,路由到 chat 模型);引用跨度上限收紧 **80→40 行**(校验拒绝+消毒钳制+补齐跳过);引用覆盖补齐与消毒后移为「终检」,verify 门控改记终态哈希;证据元话语型答案确定性拒绝。终态深度 run-0026:S 级确定性审计全过(引用精度/贴合抽样 20/20/风险给药 100%/追问闭环 300/300/难度分 100/100/flag=0),质量门禁 A+,评委自评 8.9(易用性 6.0→8.0,全维度 ≥8.0)。
- **真实 Extension Host 端到端验收自动化**(audit/extension-host-e2e.cjs,隔离窗口):空载命令、真·进行中误点去重、专属通道自动创建并弹出、阶段横幅进通道、独立 run 目录、增量 183s→48ms、Webview nonce CSP + 契约可见、产物定位——**10/10 通过**。

## 0.8.0(2026-09-16)

题库可背诵性(S 级行动):题题可背、追问闭环、难度量化、行号精确、数字可证。

- **追问链升级**:每条追问强制附带 30-80 字**参考要点**——只问不答的开放式追问被确定性校验拒绝;旧题库自动升级,渲染为「追问 → ↳ 参考要点」。
- **量化难度分**:每题给 1-10 难度(1-3 记忆/复述、4-6 原理理解、7-8 权衡设计、9-10 底层实现/极端场景),与难度标签并列展示;模型缺填时按标签确定性兜底。
- **行号精确化**:单条代码依据跨度超过 80 行(文件级/整文件式引用)被校验直接拒绝并定向重修;校验员增加"区间与所述内容贴合度"裁决,过宽/错位判 fix 并给精确行号。
- **风险当场给药**:答案要点凡指出风险/缺陷,必须同时给可执行改进方案(机制+验证方式),只预警不给改进的写法被提示词禁止。
- **叙述可证**:《项目讲解》等材料的每个性能/规模结论必须三选一标注【实测:方法+数字】/【经验判断】/【暂无数据】;1 分钟与 10 分钟讲稿共用同一组事实锚点,禁止互相矛盾的数字;结果层给可复现证据(命令 + 预期输出)。
- **评委同步**:易用性评委按新契约打分(引用精确/追问闭环/难度量化/背诵完整度);事实评委把"引用贴合度"计入评分。
- 提示词存档同步更新(18 份,docs/prompts);总装/精读门控纳入提示词全文,提示词演进自动触发重算。

## 0.7.0(2026-09-16)

候选构建(非正式发布版;因商店版本号需为纯数字,由 1.0.0-rc.1 更名而来):

- DeepSeek 完整响应超时保护、截断恢复与可诊断连接测试(命令面板「测试 DeepSeek 连接」)。
- 引入 LangGraph 生命周期断点与可选 Tavily 公开技术参考(严格与代码证据隔离)。
- 深度模式完整生成、对抗校验与题库展示质量门禁；本候选版尚未发布到 Marketplace。

## 0.5.2(2026-09-16)

输出分离:前一次与后一次的日志、产物不再堆在一起。

- **每次生成自动开启一个新的输出通道**(「代码转面试 · 仓库名 · 启动时刻」)并自动弹出;主通道保留全部运行的完整历史;移除任务记录时连同其专属通道一起清掉。
- **每次生成默认落入独立产物目录** `interview-output/runs/run-0001`、`run-0002`…(生成前可选:自动独立目录 / 固定 interview-output / 自定义目录),前后两次互不覆盖。
- 独立目录模式下增量缓存与断点自动从上一次接续:仓库没变的部分直接命中缓存,重跑几乎零成本;最终产物不携带,每个 run 目录自成完整快照。
- 「打开报告 / 产物 / 质量报告」自动定位最近一次完成的 run;CLI 的 rehearse/evaluate/topup 指向具体 run 目录即可。
- 显式指定输出目录(`--out`)时沿用旧的固定目录语义。

## 0.5.1(2026-09-16)

稳定性与多语言补丁。

- 阶段 0 新增多语言识别与常见 Dart/Rust/PHP/Prisma/Gradle/.NET 等工程依赖线索。
- 网络失败显示脱敏端点和原因码，便于排查 DNS、端口、代理、超时与 TLS。
- 推理型模型的严格 JSON 任务自动使用 `deepseek-chat`，避免 `deepseek-flash` 等模型把输出预算耗尽；项目叙述仍使用用户选择的模型。

## 0.5.0(2026-09-16)

A+ 工程升级：确定性证据链、请求策略、成本控制与报告体验。

- 新增 economy/balanced/deep 三种模式；生成前展示题量与调用范围、最大精读文件数和输出目录。
- API 客户端改为请求级预算与硬上限；非空截断不入缓存，401/403/400/404 不重试，429/408/5xx 尊重 Retry-After，取消立即生效。
- 画像过滤浏览器 profile、构建/缓存产物；新增 100–300KB 分段密钥扫描、便宜仓库快照和跳过原因统计。
- 阶段 1 大模块拆局部证据卡；阶段 2 批量与有界并发；阶段 3 动态装箱、并发校验和指纹 checkpoint。
- 新增质量门禁、主张账本、证据图、依赖清单和可复现运行清单；HTML 支持主题、组合筛选、搜索高亮、弱项练习和学习进度导出。
- Webview 结构异常时只安全降级到系统浏览器，VS Code 侧栏提示取消、缓存续接和错误类别。

## 0.4.0(2026-09-15)

品牌定名 **Code2Offer**;进度 UI 全面重做。

### 进度时间轴面板(侧栏「代码转面试」)
- 每个生成任务展开为**阶段节点时间轴**:画像 → 精读 → 出题(含修复环)→ 对抗校验 → 标红题重写 → JD → 总装 → 完成,共 8 节点
- 节点显示状态与**逐段用时**:✓ 完成 · 💿 门控命中缓存复用 · ⟳ 进行中(实时秒表)· ⊘ 跳过 · ✗ 失败 · ■ 已取消 · ○ 等待中;悬停查看每节点备注(如「pass 84 · fix 9 · flag 4 · 未覆盖 3」)
- 任务条实时汇总:已用时长、当前阶段、完成节点数;完成/失败/取消后条目保留可回看,🗑 单独移除,或一键清空记录

### 误点多次与并发控制
- **同一仓库去重**:误点 N 次不会产生第二个任务——弹窗提供「查看进度」(聚焦面板)与「取消并重新开始」(自动接回原参数)
- **不同仓库真并行**:各任务独立通知条、独立 ✕ 取消(标题栏一键全部取消);取消秒级生效,已完成阶段缓存全保留
- 内联操作按钮:运行中任务悬停出现 ✕(取消),已结束任务出现 🗑(移除)

### 其他
- CLI 更名入口:全局命令 `code2offer`(根 package.json bin);`--version` → 0.4.0
- runner 新增 `onStage` 结构化阶段事件宿主 API(CLI 行为不变)

## 0.3.0(2026-09-15)

全面安全与正确性修复(基于完整审计报告),建议所有用户升级。

### 安全
- 敏感文件拒绝清单:`.env`/私钥/凭据类文件绝不进入模型输入(独立于 .gitignore 生效),内容级密钥特征扫描兜底
- 端点与凭据同源绑定:被分析仓库的 `.env` 不能再把请求重定向到其他域名(防密钥外发),BASE_URL 强制 HTTPS(localhost 除外)
- 配置一次一解析:扩展不再写全局环境变量,多仓库各自使用自己的密钥/模型,互不污染

### 可用性(解决"运行很久没有任何反馈")
- 生成过程可取消:通知栏取消按钮,当前请求完成后立即停止,已完成阶段保留缓存
- 全部阶段日志进入专属输出通道「代码转面试」:每阶段横幅、耗时、重试警告、token 成本一目了然
- 阶段 1 模块精读并发 3、阶段 5 四份文档并行生成:推理模型下总时长大幅缩短
- 树视图状态实时刷新;记住本次生成目标,「打开报告/产物文件夹」不再找错目录

### 正确性
- 阶段门控改为内容哈希:等长修改代码、重新出题、修改答案都会正确触发重算(旧版会整链命中旧缓存)
- 校验闭环:模型失败/漏答的题标记 `未覆盖` 而非"通过";校验断点按输入指纹复用,不再把旧裁决套到新题
- 100 题配额:补题按"类别×难度缺口"定向回补,类别漂移被强制纠正;`topup` 有上限保护
- JD 分析输出形状校验:坏形状不再写入缓存造成"重跑必崩"
- 标红题重写保留原始裁决记录,校验报告按最终题库重算

### 报告
- 交互改为事件委托(无内联脚本):Webview 内搜索/筛选/自测标记全部可用,浏览器与 VSCode 行为一致
- 掌握度进度按仓库隔离(localStorage 命名空间),不同项目互不串扰
- 搜索覆盖追问链/加分回答/易错回答/对比表,150ms 防抖
- Markdown 表格单元格清洗:`|` 与换行不再破坏表格结构

### CLI
- 参数解析重写:`--top20 <目录>` 等布尔旗标在前不再吞掉路径参数;`--force <仓库>` 不再静默失效;新增 `--version`/`--help`
- 排练逐题落盘:中途退出不丢已答成绩;评分钳制 0-10,非法评分不再记 0 分污染弱项统计
- 自评环评委结果缓存:同一份材料重跑 evaluate 分数可复现;评委失败按"未评估"归一化,不再双重惩罚

## 0.2.3
- 2.7 引用消毒、3.5 标红题重写、对比块补齐/要点实质化修复环

## 0.2.0 – 0.2.2
- 六阶段管线成型:画像→精读→出题→对抗校验→JD 加权→总装

## 0.1.0
- 首版:CLI + 扩展薄壳,百问百答与 HTML 报告
